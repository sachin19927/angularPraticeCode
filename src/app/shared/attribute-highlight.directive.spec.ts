import { AttributeHighlightDirective } from './attribute-highlight.directive';

describe('AttributeHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
