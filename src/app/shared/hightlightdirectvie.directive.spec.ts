import { HightlightdirectvieDirective } from './hightlightdirectvie.directive';

describe('HightlightdirectvieDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightdirectvieDirective();
    expect(directive).toBeTruthy();
  });
});
