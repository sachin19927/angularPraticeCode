import { StrcaseDirective } from './strcase.directive';

describe('StrcaseDirective', () => {
  it('should create an instance', () => {
    //const directive = new StrcaseDirective();
    //expect(directive).toBeTruthy();
  });
});
